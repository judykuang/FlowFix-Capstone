/**
 * Legacy entry: shared UI has moved to `button-behavior.js`.
 * Keep this file so older bookmarks or forks that still reference script.js get a clear signal.
 * Prefer: <script src="button-behavior.js" defer></script>
 */
console.info("[FlowFix] Load button-behavior.js for navigation, modals, theme, and chat.");
